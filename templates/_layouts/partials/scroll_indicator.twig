<div class="h-1 bg-background dark:bg-gray-400"
     @scroll.window="scroll()"
     x-data="{
        scroll() {
            var winScroll = document.body.scrollTop || document.documentElement.scrollTop;
            var height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            this.$refs.indicator.style.width = Math.min((winScroll / height) * 100, 100) + '%'
        }
     }">

    <div x-ref="indicator" class="h-1 bg-foreground dark:bg-gray-200" style="width: 0%"></div>
</div>
