{% set padding = padding is defined ? padding : true %}
{% set tag = tag ?? 'div' %}

{% tag tag with {
    class: "max-w-screen-md mx-auto #{padding ? 'px-4 sm:px-8'}"
} %}

    {% block content %}{% endblock %}

{% endtag %}
