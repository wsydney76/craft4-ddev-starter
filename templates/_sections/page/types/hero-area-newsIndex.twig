{% if craft.app.request.pageNum == 1 %}
    {% set featuredEntry = craft.entries.section('news').one %}
    {% if featuredEntry %}
        {% include '_layouts/partials/hero-area-entry.twig' with {
            entry: featuredEntry,
            teaser: 'Latest News'|t
        } only %}
    {% endif %}
{% endif %}